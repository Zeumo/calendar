{
  "name": "calendar",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "webpack -wd & npm run watch-styles",
    "build": "webpack -p && npm run styles",
    "build-styles": "node-sass ./src/*.scss -o ./dist",
    "watch-styles": "node-sass -w ./src/*.scss -o ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "expose-loader": "^0.6.0",
    "jquery": "^2.1.3",
    "lodash": "^3.6.0",
    "node-sass": "^3.0.0-beta.4"
  }
}
